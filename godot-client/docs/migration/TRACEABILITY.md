# Migration traceability matrix

Status: NOT_STARTED, FOUNDATION, IMPLEMENTED, VERIFIED, BLOCKED.

| Legacy feature | Source | Godot target | Test/evidence | Status |
|---|---|---|---|---|
| TCP framing | client connection; server protocol.py | network/protocol.gd | byte fixtures + fragmented/combined reads | FOUNDATION |
| Configurable endpoint | servers.c | responsive login scene/config | rendered CI connected to `18.235.240.60:2000` through the real login/creation UI | VERIFIED |
| Login | loginwin.c; LOG_IN | auth/network | disposable character creation and subsequent login against the development server; credentials redacted | VERIFIED |
| Actor spawn/update/remove | multiplayer.c; server actor packets/commands | state/actors | actor packet + command-step fixtures; rendered two-client native spawn, authoritative movement, ray selection, and disconnect cleanup | VERIFIED |
| NPC activation/dialogue | gamewin.c; server protocol/world | protocol/state/dialogue UI | byte/decode fixtures plus local unmodified-server enhanced NPC, activation, reply, and close; development Four Gates rendered dialogue remains pending | IMPLEMENTED |
| Chat send/receive + PM | legacy `text.c`, `chat.c`, `pm_log.c`; server `protocol.py`, `server.py`, `world.py` | exact RAW_TEXT/SEND_PM codecs, channel state, safe lower-left presentation | exact bytes and channel/color/UTF-8 fixtures; local PM contract; rendered bidirectional development-server local chat and unobscured lower-left UI | VERIFIED |
| Title/login artwork | generated Eloria branding DDS | portable PNG copies + responsive themed login/creation | 1280x720 bounds fixture covers login and creation action rows; user screenshot comparison | IMPLEMENTED |
| GLB map + JSON | GLB runtime/map.c | world/map loader | `four_gates` alias fixture, headless attach, and rendered development-server Four Gates capture | VERIFIED |
| Native luminous models | actor GLB runtime/assets | actor presentation | rendered female luminous GLB with three visible native meshes and no fallback | VERIFIED |
| Movement reconciliation | client movement/server authority | controller/state | rendered real-server MOVE_TO changed authoritative tile `(768,480)` to `(773,481)`; actor yaw and camera followed | VERIFIED |
| Sit/stand | legacy `gamewin.c`, `keys.c`, `multiplayer.c`, `client_serv.h`; server `protocol.py`, `server.py`, `world.py` | exact desired-state codec, actor reducer, native transition/rest animation state | exact `07 02 00 01` / `07 02 00 00` fixtures; rendered real-server explicit sit/stand and automatic stand-on-move | VERIFIED |
| Core HUD/chat | `hud.c`, `hud_misc_window.c`, `gamebuttons*.dds` | ui/hud | lower action/window rail; right stats and item quickbar, with the spell quickbar on the left as the legacy client has it; unsupported windows visibly disabled | IMPLEMENTED |
| Inventory | `items.c`, `items.h`, `hud_quickbar_window.c`; server `protocol.py`, `world.py`, `items.py` | protocol/state/inventory UI | exact snapshot/update/remove/use/move/cooldown fixtures; two-click placement; rendered real-server 3-item snapshot with 3/3 visible icons and quantities | VERIFIED |
| Equipment | `items.c`, `items.h`; server `protocol.py`, `world.py`, `items.py` | equipment slots and actor attachment presentation | exact wear-slot/move fixtures; rendered real-server spear equip/unwear, slot reconciliation, actor visual, skeleton fallback, and cleanup; the generic tier claims the legacy visual-id space directly, so `0:11`, `1:5`, and `2:11` render as `STAFF_4`, `SHIELD_BRONZE`, and `CAPE_GOLD`; the alias table that redirected them to Four Gates guard gear is gone and bespoke NPC gear comes from `npcLooks`, which names native ids; rendered native verification remains pending | IMPLEMENTED |
| Character statistics | legacy statistics/HUD code; server `protocol.py`, `stats.py`, `world.py` | authoritative reducer, resource rail, statistics window | rendered real-server health, ether, food, carry, attributes, and skills; visually inspected non-overlapping 1280x720 window | VERIFIED |
| Item quickbar | `hud_quickbar_window.c`; server inventory state | first eight authoritative inventory positions | rendered real-server 3/3 populated quick items with visible icons and quantities | VERIFIED |
| Combat | `gamewin.c`, actor command handling; server `protocol.py`, `server.py`, `world.py` | selected-target attack action, health/combat/death replication | exact attack/damage/heal fixtures; local server approach, facing, enter-combat, and attack commands | IMPLEMENTED |
| Player trade | legacy `trade.c`, `items.c`, `multiplayer.c`; server `protocol.py`, `server.py`, `world.py` | exact codecs, reducer, selected-player action, three-column offer window, per-slot inventory/storage destinations | exact request/offer/remove/inspect/two-phase accept/reject/exit/destination fixtures; reducer cleanup; local two-client TCP request, offer, restoration, cancel, and completion verified | IMPLEMENTED |
| Storage | legacy `storage.c`, `multiplayer.c`; server `protocol.py`, `server.py`, `world.py`, storage NPC dialogue | exact codecs, reducer, category/stored/inventory window | exact category/deposit/withdraw/inspect fixtures; local TCP NPC activation, open, category selection, deposit, inspect, withdrawal, and inventory restoration | IMPLEMENTED |
| Ground bags | legacy `bags.c`, `bags.h`, `gamewin.c`, `multiplayer.c`; server `protocol.py`, `server.py`, `world.py` | exact codecs, map-scoped reducer, pickable grounded marker, contents/inventory window | exact create/list/inspect/item/remove/close/destroy fixtures; local TCP drop, create, open, partial pickup, close/reopen, final pickup, destroy, and inventory restoration | IMPLEMENTED |
| Knowledge/books | legacy `knowledge.c`, `knowledge.h`, `multiplayer.c`; server `knowledge.py`, `protocol.py`, `server.py`, `world.py`, `config/items.txt`, `config/books.txt` | hashed data-driven catalog, ownership reducer, inspection window | exact bitset/new/index/text fixtures and UI bounds; local TCP fresh-character 49-byte ownership bitset and first/last catalog inspection | IMPLEMENTED |
| Magic | `spells.c`, `hud_quickspells_window.c`; server `magic.py`, `protocol.py`, `world.py`, `config/spells.xml` | exact cast codec, sigil/result/buff state, separate spell quick slots | cast/sigil/result/availability fixtures; live owned-spell success pending | IMPLEMENTED |
| Manufacturing | legacy `manufacture.c`, `manufacture.h`, `items.c`, `multiplayer.c`, `client_serv.h`; server `recipes.py`, `knowledge.py`, `protocol.py`, `server.py`, `world.py`, `config/recipes.txt`, `config/items.txt`, `config/books.txt` | exact codec, hashed 389-recipe catalog, authoritative inventory-slot resolver, filter/detail/action window | exact packet/catalog/availability/UI bounds fixtures; local TCP exact-selection rejection plus successful Fire Essence mix with raw-text, inventory, item-text, and partial-stat responses | IMPLEMENTED |
| Harvesting | respective modules; server harvest-node manifests | reducer/window and world-object selection | action/result traces; Four Gates harvest object IDs unavailable | NOT_STARTED |
| Quest journal | legacy `questlog.c`, `quest_journal.c`, `multiplayer.c`, server command 224 contract | server-owned journal reducer/window | **No longer blocked.** The claim that the server does not emit command 224 was wrong: it emits it as soon as `quest_journal_v1` is advertised, and falls back to raw text otherwise. Proved in `tests/integration/capabilities_actor16_local.py`, which asks `#quests` before and after `#clientcaps` on a real server. The missing `#clientcaps` (1.1) was the entire cause. The window itself is Phase 2.1 work; dialogue inference remains rejected | NOT_STARTED |
| Hotkeys/settings | keys.c/elconfig.c | InputMap/settings | viewport routing fixture verifies rotate/pan/zoom; live click-to-move recheck pending | FOUNDATION |

## Phase 0 repairs

Code that already existed and did not work, so each item read as done from the
outside. Status here is per repair, not per legacy feature.

| Item | Defect | Fix | Test/evidence | Status |
|---|---|---|---|---|
| 0.1 Marker lights never bind | `_bind_light_markers()` had no call site; `map_light_root` stayed null for the whole session and every manifest `lighting.markers` block was dropped. `LightMarkerBinder` was exercised only from a rendered integration script, so CI stayed green. | `_on_world_loaded()` now binds the rig between the environment binder and ambient life; `_clear_world_presentation()` frees it. | `tests/test_world_lighting.gd` drives the real `_on_world_loaded()` entry point: two declared markers bind with their declared name/position/energy/range/colour, an unhinted marker takes the documented defaults, a second load replaces the rig, and a manifest with no markers leaves nothing parked in the world. Added to CI alongside the previously unwired `test_runtime_performance.gd`. Structural assertion only; rendered before/after capture pending. | IMPLEMENTED |
| 0.3 Map viewports fought their own throttle | `_sync_map_viewport_activity()` set `UPDATE_ALWAYS` whenever a map panel was visible, while `_update_map_viewports()` set `UPDATE_ONCE` on a 15 Hz / 5 Hz schedule. It runs on every map load and every full-map toggle, so a visible minimap returned to full-rate world rendering for up to one throttle interval. | `_sync_map_viewport_activity()` now only ever sets `UPDATE_DISABLED` or `UPDATE_ONCE`; the throttle owns the schedule. Both toggles and both tab-map view switches call it, so a hidden or continent-view panel idles immediately. | `tests/test_runtime_performance.gd` covers the map-load path with panels visible and hidden, the tab-map open/close toggle, the continent/current view switch, and the minimap toggle. Structural assertion. | IMPLEMENTED |
| 0.7 Unreachable and shadowed InputMap actions | `connect` was never queried. `disconnect` had an empty event array and no handler. `toggle_inventory`, `turn_left` and `turn_right` were declared actions bypassed by raw keycode checks, so rebinding them appeared to work and did nothing. `toggle_map`, `toggle_minimap` and `toggle_console` were handled twice, once by action and once by keycode. | `_input()` delegates to a new `_handle_bound_action()` built entirely from `is_action_pressed()`; the duplicate action branches are gone from `_unhandled_input()`; `_turn_step_for_key_event()` is deleted; `disconnect` is bound to Ctrl+D and calls `_on_disconnect_pressed()`; a focused text field suppresses printable bindings so chat keeps its own characters and clipboard shortcuts. | `tests/test_world_input.gd` rebinds `turn_left`, `turn_right`, `toggle_map`, `toggle_minimap` and `toggle_console` to F9 and asserts the old key stops working, the new key works, and restoring the default restores the old key - which a raw keycode path cannot satisfy. It also asserts all nine actions carry at least one real event, that both connection bindings reach a handler, and that a bound printable key typed into chat is inert. | IMPLEMENTED |
| 0.6 Trade acceptance was inferred | `app_state.gd` computed `own_accepts`/`other_accepts` as `mini(2, n + 1)`, counting `GET_TRADE_ACCEPT` packets instead of reading the phase off the wire. Any duplicate, dropped or reordered accept desynchronised the two-phase state machine from the server. | Server `p.trade_accept()` now sends `other:u8 \| phase:u8` and `World.accept_trade()` passes the accepting session's authoritative `trade_accept_state`. The client decodes the phase, rejects a legacy one-byte frame and an out-of-range phase, and the reducer assigns the phase instead of incrementing. | Client: `tests/test_protocol.gd` byte fixtures for both sides and both rejections; `tests/test_world_input.gd` drives a duplicated accept, an out-of-order pair and the second stage through the reducer. Server: `tests/test_trade_accept_phase.py` covers the packet builder, clamping, and which phase each side is told across a full two-stage sequence. Structural assertions; live two-client rendered evidence pending. | IMPLEMENTED |
| 0.2 Turning was cosmetic | Q/E called `ReplicatedActor3D.turn_by()`, which rotated the local mesh and set a facing override. `TURN_LEFT(11)`/`TURN_RIGHT(12)` were not in `ClientMessage` at all and the server did not handle them, so the server never learned the facing, no other player saw the turn, and the local view disagreed with authoritative state until the next movement command. | Client sends the turn and keeps only a prediction: `predict_turn()` shows one step, and the first authoritative turn command clears the override so the server's facing wins. Server handles 11/12, rotates one 45 degree step, stores the facing in the actor packet's signed 16-bit rotation field, broadcasts `CMD_TURN_*` to the map, keeps rotation in sync with each walked step, and refuses to turn a seated player. | Client: `tests/test_protocol.gd` encoder bytes and turn-command classification; `tests/test_world_input.gd` proves an authoritative turn clears the prediction and later movement follows authoritative facing. Server: `tests/test_turn_facing.py` covers the facing helpers, signed-16-bit packing for all eight directions, the broadcast, and the seated guard. End to end: `tests/integration/turn_facing_local.py` drives two real clients against a real server - one turns, the other receives the same `CMD_TURN_*`, and a resync reports the turned facing. Rendered capture still pending. | IMPLEMENTED |
| 0.4 Perks were scraped from chat | `_request_perks()` sent `#list_perks` and `_capture_perks_from_chat()` pattern-matched the reply against a hardcoded 33-name array inside an eight-second window. Any renamed, added or reworded perk was silently dropped, and a slow server dropped all of them. | New server packet `ELORIA_PERKS(234)` carries `from_gear:u8 \| pickpoints:i16le \| name NUL \| description NUL` per perk, pushed at login and on every perk or equipment change. The client decodes it into `AppState.perks` and the statistics window reads that. The scraper, the request, the eight-second window and the `PERK_NAMES` table are deleted. | Client: `tests/test_protocol.gd` byte fixtures including a negative pick-point cost, a gear-granted perk, an empty list and three rejection paths; `tests/test_world_input.gd` proves a perk the client has never heard of is still reduced and presented, and that an empty packet clears it. Server: `tests/test_perks_and_counters.py` covers the packet, catalog text and source flag, and an unknown perk name. End to end: `tests/integration/perks_counters_local.py` shows the packet arriving unasked at login from a real server. | IMPLEMENTED |
| 0.5 Counters counted the wrong event | `_increment_counter()` fired when the client sent a request, so a rejected deposit or a failed mix still incremented the lifetime total, and seven of the seventeen categories (Kills, Deaths, Breakages, Crit Fails, Harvests, Spells, Summons) were never incremented at all because the client cannot observe those events. Totals were persisted in the client's own config file. | New server packet `ELORIA_ACTIVITY_COUNTERS(235)`: a full seventeen-category snapshot at login and a delta per confirmed event. `Character.activity_counters` is persisted in the character database. Increments are placed after the server commits each outcome. The client's nine `_increment_counter()` call sites, the helper, the `COUNTER_CATEGORIES` constant and the config persistence are deleted; the window builds its category list from the server and derives its session column as a difference. | Client: `tests/test_protocol.gd` snapshot/delta/saturation/rejection fixtures; `tests/test_world_input.gd` proves the category list is server-built, a delta updates one total, and a session reset keeps the authoritative lifetime total. Server: `tests/test_perks_and_counters.py` covers the snapshot listing all seventeen categories once, delta-only publication, saturation, and that every recipe skill in the catalog has a counter. End to end: `tests/integration/perks_counters_local.py` shows a refused drop moving no counter and a confirmed drop moving only Drops, by the dropped quantity. PvP kills are not yet counted; only creature and summon kills are. | IMPLEMENTED |
| 0.8 Protocol diagnostics had no listener | `unknown_packet_count` and `recent_protocol_errors` were reduced in `AppState` and emitted with no listener, so every undecoded packet and every decode failure was completely silent. That is how most of the gaps in this programme went unnoticed. | `AppState` now keeps per-opcode counts (`unknown_packets`) and structured decode failures (command, error, payload size, timestamp), plus the last clock-sync time. The console panel gained a `Protocol` toggle that swaps the message history for a diagnostics view listing undecoded opcodes with counts, recent decode errors newest first, and the connection/clock state. The diagnostics path runs before the authenticated gate in the state-changed handler, because a handshake decode failure happens while unauthenticated. | `tests/test_world_input.gd` sends two frames of an unknown opcode and a pre-0.6 one-byte trade accept on an unauthenticated session, then asserts the opcode is listed once with its count, the decode error names the packet and the failure, the panel fits within 1280x720 without covering the fixed resource rail, and it still answers the cancel cascade. Rendered before/after capture pending. | IMPLEMENTED |
| 0.10 Two papercuts | `AppearanceVariants.equipment_visuals()` returned `{}` unconditionally with both parameters underscore-prefixed, yet was called on every actor build. The minimap persisted its position and scale but not its visibility, so every session started hidden with Alt+M as the only way back. | The function is deleted and the refusal - creation bytes select skinned surfaces authored into the GLB and are never rigid attachments - is stated at the one call site that builds actor presentation. Minimap visibility is saved with the other HUD settings, restored on entering the world, and written on every toggle. | `tests/test_protocol.gd` asserts the unconditionally empty function no longer exists. `tests/test_world_lighting.gd`-style fresh-scene suite `tests/test_hud_persistence.gd` toggles the minimap in one scene instance and proves a second instance loads the setting, restores it on entering the world without Alt+M, does not put the map viewport back on continuous redraw, and persists the hidden state too. | IMPLEMENTED |
| 0.9 Decoded fields with no consumer | Eight fields were decoded and read by nothing: the NPC portrait byte, the actor frame, `researching`/`research_total`/`research_completed`, trade `source_type`, inventory `uid`, the sigil raw masks, `server_timestamp`, and cooldown `maximum_msec`. | Consumed: the actor frame now decides whether an actor arrives already in combat; the research statistics render as reading progress with the knowledge title and a percentage; trade `source_type` labels an offer that came from storage; `server_timestamp` and its arrival time drive the 0.8 diagnostics panel and a `clock` case now exists in the state-changed handler; `maximum_msec` draws a proportional cooldown drain over each quick slot, updated every frame, with the seconds remaining on it. Deleted: the NPC portrait byte (Eloria has no portrait artwork and will not convert the Eternal Lands set), the inventory `uid` and the whole ten-byte entry form (this server never emits it, and it is now rejected rather than half-decoded), and the sigil raw masks (the decoded ownership list is the DTO). | `tests/test_protocol.gd` asserts each outcome: two actor frames producing different combat state, no `portrait` key, no `low_mask`/`high_mask`, no `uid` and a rejected ten-byte entry, a storage-sourced trade offer, and a cooldown carrying its full duration. `tests/test_world_input.gd` asserts the reading-progress line for both no-book and mid-book, and that the cooldown art covers the whole slot at full duration, a quarter of it at a quarter remaining, states the seconds, and clears when expired. | IMPLEMENTED |

## Phase 1 (P0)

| Item | Finding / change | Test/evidence | Status |
|---|---|---|---|
| 1.1 Capability handshake | The Godot client sent no `#clientcaps`, so it was served the legacy fallback path for every Eloria extension. Outcome (a) was chosen: the client advertises only capabilities whose packets it actually decodes, and the list grows in the same commit that lands each window. Outcome (b) - deleting the gating and making modern behaviour unconditional - was **revisited at the end of Phase 2.1 and rejected**, with all nine windows in place. Three reasons: a session has no capabilities until its `#clientcaps` arrives, so the fallbacks are still the only correct answer for the window between connect and the handshake; the risk the gate was guarding against - advertising a packet the client cannot read - is now caught by `capability_probes` in `tests/test_protocol.gd` rather than by the gate; and deleting working server behaviour buys the Godot client nothing, since it advertises all ten and never takes a fallback. The gating stays. The original deferral reasoning follows: the fallbacks are load-bearing *for the Godot client*, not only for the retired C client. Removing them today would leave the merchant and the Nymara Exchange with no NPC-dialogue path and no window, so a player could not trade or use the exchange at all. | `tests/test_protocol.gd` pins the frame and asserts nothing is advertised whose packet the client cannot decode. `tests/integration/capabilities_actor16_local.py` drives a real server: the quest journal is raw text without `#clientcaps` and command 224 after it. | IMPLEMENTED |
| 1.2 16-bit actor types | The stated blocker was wrong in two ways. `actor16_v1` does not gate anything - measured against the real server, `actor_packet()` picks `ADD_NEW_ACTOR_EXTENDED(247)` purely from `actor_type > 0xFF`, so 16-bit actors were already on the wire for a client that advertised nothing. The real blocker was that **no creature ever spawned**: `spawns.txt` was loaded into `World.spawn_definitions` and used for nothing, the independent profile enables no `.def` spawn groups, and the only creatures reachable were summons and invasions. Server change: `World.populate_static_spawns()` instantiates every entry, joining the normal death/drop/respawn cycle. | Server: `tests/test_static_spawns.py` covers population, skipping unknown maps and creatures without a fatal boot, and that every shipped spawn names a buildable map and creature with types above the 8-bit ceiling. End to end: `capabilities_actor16_local.py` receives types 401 and 402 on command 247, asserts no creature above 255 used the 8-bit packet, and attacks one to get authoritative combat commands and damage. Rendered: `tests/integration/rendered_actor16_creature.gd` decodes the exact captured server frames through the client's own protocol, model-resolution and actor-build path. | IMPLEMENTED |
| 1.5 Keepalive and resync | The client answered `PING_REQUEST(60)` but never initiated `HEART_BEAT(14)`, and had no reconnect, relogin or resync path: a dropped socket ended the session with a bare "Disconnected". Server change: every inbound packet is timestamped and a sweep closes a logged-in connection silent for `client_idle_timeout_seconds` (default 90), because a half-open socket otherwise holds a character in the logged-in set and blocks its own owner's reconnect. | Client sends the 25-second heartbeat, reconnects the socket with 1/2/4/8/15s backoff after an *unexpected* drop only, shows the state in a HUD banner, and sends the three resync snapshots after re-authenticating. Credentials are still never retained: a recovered socket returns to the login panel with the username preserved. | `tests/test_protocol.gd` pins all four zero-payload frames. `tests/test_connection_lifecycle.gd` asserts the cadence, monotonic backoff, that a deliberate disconnect schedules nothing, that an unexpected drop announces its attempt, that the banner fits 1280x720 clear of the resource rail, and that reconnect gives up rather than retrying forever. Server: `tests/test_idle_client_eviction.py` covers eviction, the live and not-yet-authenticated exemptions, the zero-timeout disable, and that the shipped timeout leaves room for several heartbeats. End to end: `tests/integration/keepalive_resync_local.py` keeps a client alive past twice its idle timeout by heartbeat alone, gets all three resync snapshots back, watches a silent client be closed, and logs the evicted character straight back in. | IMPLEMENTED |
| 1.6 Server popups | `DISPLAY_POPUP(83)` was enumerated but fell through to `{"type":"unknown"}` and `POPUP_REPLY(50)` had no encoder, so the server had no way to ask the player a question. Both directions are implemented with all four legacy option types, plus a modal window that closes the panels underneath it, takes the cancel cascade ahead of everything else, and follows the legacy send-button rule. | `tests/test_protocol.gd` decodes the summon-behaviour popup byte for byte as the server's own `summon_behavior_popup()` builds it, covers a text-entry popup, and rejects a truncated payload, unsupported flags and an unimplemented option type; four reply fixtures cover a choice, a text entry, multiple groups and an empty answer. `tests/test_world_input.gd` proves the window is modal, enforces one selection per group by server value rather than button index, fits 1280x720 clear of the resource rail, answers the cancel cascade, ignores a repeated popup id, and leaves the popup open when the reply could not be sent. Server: `tests/test_popup_round_trip.py` feeds the client's exact reply bytes into the server's dispatch and reaches the summon-behaviour handler. Rendered: `server-popup.png`, human-inspected. | IMPLEMENTED |
| 1.3 Harvesting and map-object interaction | The only NOT_STARTED row. `HARVEST(21)`, `USE_MAP_OBJECT(16)` and `LOOK_AT_MAP_OBJECT(27)` were enum values with no encoder, and there was no world-object pick path: the world click handler tried actors, then ground bags, then the navigation surface, and stopped. Server changes: `ELORIA_MAP_OBJECTS(236)` states which object ids exist on the map, where, and what they require, chunked because a busy map has thousands of nodes; `ELORIA_HARVEST_STATE(237)` reports harvesting explicitly instead of the client matching an English chat phrase; `inspect_map_object()` answers with a real description rather than a maps.txt template. Client: the three encoders, a `MapObject3D` pick layer of its own, the harvest indicator, and a diagnostics count of server objects with no navigation surface beneath them. Identity is server-declared rather than manifest-declared *and* never filename-matched - recorded as a deliberate departure from the suggested manifest-metadata route, because a manifest cannot know the server's object ids. | Client: `tests/test_protocol.gd` pins all three encoders and both new packets including four rejection paths; `tests/test_world_input.gd` proves every server object becomes a pick target on its own collision layer at the server's tile, that harvest nodes and interactives are told apart by the server's kind, that the indicator follows the authoritative state and clears on a server stop, that it fits 1280x720 clear of the resource rail, and that a map change discards the previous map's objects. Server: `tests/test_map_objects.py` covers the entry contents, a stated tool requirement, per-map filtering, and both harvest-state directions. End to end: `tests/integration/harvesting_local.py` gets the object list unasked at login, looks at a node, harvests real items, and proves moving away produces an explicit stop. Rendered: `harvest-targets-idle.png` / `harvest-targets-active.png`, human-inspected. | IMPLEMENTED |
| 1.3 four_gates harvest node placement | **Content defect, not a client gap.** Every one of the 22 shipped `four_gates` harvest nodes sits in a coordinate space the rest of that map's content abandoned: the nodes are clustered inside a ~170-tile square while the map's interactives, portals and player spawn are all on the 1536-tile grid (spawn `(768,480)`, storage `(768,1424)`). The four-gates world package is no better: converting its four declared `harvestables` through the documented transform yields tiles `(896,126)`, `(-136,135)`, `(208,-171)` and `(582,926)` - two of them outside the server grid entirely. Harvesting is proved against a probe node placed beside the spawn; the shipped placement needs regenerating on both sides before it is reachable in play. `tests/test_map_objects.py::test_the_shipped_four_gates_harvest_nodes_are_misplaced` pins the exact numbers so a fix is recognisable. | Numbers above, measured. No client change can correct it. | BLOCKED |
| 1.4 Books - the reading half of the knowledge loop | A player could not see that they were reading anything: the three research statistics arrived and nothing read them, and the manufacturing resolver reported "unread knowledge" as a blocking reason it had no way to clear. `AppState.reading` is now a typed reduction of `researching`/`research_completed`/`research_total`, and a reading window reports the book, the pages and the progress, then what the knowledge granted when it finishes. Hiding the window does not interrupt reading, because the server has no command to stop it and the client does not pretend otherwise. | `tests/test_world_input.gd` proves the reduction from real partial-statistic bytes, the window contents and progress bar, 1280x720 bounds clear of the resource rail, that a knowledge-gated recipe's block clears once the bit is known, that the bit comes from `GET_NEW_KNOWLEDGE(56)` rather than being inferred from completion, and the completed presentation. End to end: `tests/integration/books_local.py` grants a real book, uses it, watches reading start and report its page count, and receives the knowledge packet on completion. Rendered: `reading-progress.png` / `reading-complete.png`, human-inspected. | IMPLEMENTED |

| 1.4 Legacy page-turning book protocol | `OPEN_BOOK(64)`, `READ_BOOK(65)`, `CLOSE_BOOK(66)` and `SEND_BOOK(43)` are deliberately **not** implemented. This server has no book text at all: `config/books.txt` is a two-setting tuning file and `load_books` derives a definition from each book *item* with a page count and nothing else. A page-turning window would be a window with nothing behind it. The server change needed first is book content; those four opcodes are then the way to carry it. | Recorded, not built. | NOT_STARTED |
| 1.4 Knowledge catalog does not match the Eloria server | **Content defect.** The client's 385-entry knowledge catalog and the knowledge-gating half of its 389-recipe manufacturing catalog were generated from the legacy Seridia configuration (`config/items.txt` + `config/books.txt`, which yields exactly 385 non-repeatable books). The independent Eloria profile (`config/eloria/*`) contains **one** book: `Book of Beginnings`, knowledge `Beginnings`, index 0. So the client's index 0 is `Metallurgy` while the server's is `Beginnings`, and 199 recipes are gated on indices up to 381 that the Eloria server can never grant. The reading loop is proven end to end against the real book; "a recipe previously blocked on that knowledge becomes mixable" is proven only in the client's resolver, because the live content cannot demonstrate it. Regenerating the client catalogs from `config/eloria` is the fix. | Measured: 385 vs 1 books; max gated index 381. | BLOCKED |
| 1.4 books.txt escape defect | **Content defect.** `config/eloria/books.txt` separates its lines with literal backslash-n escapes rather than newlines, so the whole file parses as one comment and both settings silently fall back to library defaults: `default_pages` becomes 1000 instead of the intended 600 and `big_book_experience` 60000 instead of 3000. Found because a probe could not shrink the page count by editing the file. | Measured by loading it: `BookDefinition(..., pages=1000)`. | BLOCKED |

### Verified rendered evidence: 16-bit creatures

`actor16-native.png` / `actor16-fallback.png` are rendered from the exact
`ADD_NEW_ACTOR_EXTENDED(247)` frames captured from a real local server for a
Reedhorn Stag (type 401) and a Four Gates Turtle (type 402). Nothing in the
script hand-builds an actor the wire could not produce: the bytes go through
`EloriaProtocol.decode_server()`, `_model_for_actor()`, `_presentation_dto()`
and `ReplicatedActor3D.configure()`.

Human inspection of both frames: the native frame shows the stag on the `elk`
GLB with antlers, fur shading and a cast shadow, and the turtle on the
`desert_tortoise` GLB with its shell plating, both nameplated with the
authoritative server names and both playing their native `idle` clip. The
fallback frame - the same two decodes with no model registry entry - shows two
magenta missing-model capsules, which is what a client unable to express a
16-bit type would render. The structural assertions additionally confirm each
actor has a `NativeModel` node, no `MissingModelFallback`, and a non-empty
current animation clip.


## Phase 2 (P1)

Three rows below carry a **scope correction**. The programme brief was out of
context for part of Phase 2, and items 2.6, 2.9 and part of 2.2 were rebuilt
from their titles rather than from the opcode lists the brief gives. What was
delivered under 2.6 is a quest-pinning HUD; what was delivered under 2.9 is
`SEND_SPECIAL_EFFECT(79)`, which belongs to 3.1. Both stand on their own and
are renumbered here rather than left mislabelled, and the items as actually
specified are recorded blocked with the evidence beside them.

| Item | Finding / change | Test/evidence | Status |
|---|---|---|---|
| 2.1 The nine Eloria extension windows | Commands 222-232 were enumerated and every one of them fell through to `{"type":"unknown"}`: the marketplace, merchant, quest journal, item detail, inventory state, combat state, mail, navigation and special-event packets all arrived and nothing read them. All nine now decode into typed state slices and render in `src/ui/extension_windows.gd`, which holds no state of its own - each window is one server-push snapshot. `CLIENT_CAPABILITIES` grows from one string to ten in the same commit as the windows. Command 226 is deliberately **not adopted as the inventory**: the authoritative inventory packet still owns slots and quantities, and 226 is used only as enrichment for the tooltip (name, category, EMU), so the two can never disagree about what is in a slot. Server change: `show_shop_quantities` no longer draws the legacy NPC dialogue for a `merchant_window_v1` client - it recorded the selection and drew a text window plus an options menu over the very window the player was trading in - and the three refusal paths route through one `refuse_shop_trade` helper that answers each client in its own window. | Client: `tests/test_protocol.gd` decodes all nine from byte-exact server output and rejects eight truncated payloads; a `capability_probes` table proves every advertised capability decodes a real payload, so nothing can be advertised that the client cannot read. `tests/test_extension_windows.gd` (new suite, wired into CI) asserts each window's content, that all seven panels fit 1280x720 clear of the fixed resource rail, that server-opened windows precede player-opened ones in the cancel cascade, that toggled windows are mutually exclusive, that a listing row carries the server's listing id rather than its row index, that a defeat ends the combat engagement rather than freezing the last frame, and that a disconnect clears every window. `tests/test_world_input.gd` proves the 226 enrichment keeps the authoritative quantity when the two packets deliberately disagree, and falls back to the image id with no enrichment entry. Server: `tests/test_merchant_window_dialogue.py` - a capable session receives no frames at all from `show_shop_quantities` while keeping its selection, a legacy session still gets `NPC_TEXT` and `NPC_OPTIONS_LIST`, and a refused trade reaches each client in its own window. End to end: `tests/integration/extension_windows_local.py`, 13/13 against the real server - 222/224/226/229/230 answer the capable session while the plain session gets the legacy text, touching a shop NPC opens command 223 with **no** NPC dialogue frames, and a real purchase ("You bought 1 Bread for 8 gold coins.") answers with a refreshed merchant window rather than a menu. Rendered: `extension-windows-before.png` and six after-frames, human-inspected. | IMPLEMENTED |
| 2.2 (b) Server-triggered audio | **Not on the wire.** The brief scopes `PLAY_SOUND(14)` and `PLAY_MUSIC(54)` into 2.2; this server allocates neither in the server-to-client direction. The audio that is implemented is therefore answered entirely to packets that do exist - the harvest state, the inventory snapshot, the combat state and `SEND_SPECIAL_EFFECT` - which is what 2.2 delivers. Footsteps and per-map ambient music are reachable without either opcode and are outstanding rather than blocked. | Server: `tests/test_unimplemented_opcodes.py` pins both. | BLOCKED |
| 2.2 Audio | The client had no audio at all - no sounds, no mixer, no setting. Adds `eloria-assets/tools/build_native_sounds.py`, which synthesizes the whole sound set from noise and sine partials with a fixed seed per sound, and `src/audio/audio_director.gd`, which plays them in answer to authoritative events only: a harvest the server started, an item it put in the backpack, a combat outcome it reported, an effect it announced, and the player's own clicks. A snapshot that restates what was already true is not a new event, so a repeated harvest state or a repeated combat outcome is silent. Sound on/off and volume live in the same settings file as the rest of the HUD preferences. Every sound is original and generated: nothing is sampled, converted or traced, and both the tool and its output are recorded in `eloria-assets/provenance.json`. | `tests/test_native_sounds.py` proves the catalog lists exactly what the generator makes, that each file is mono 16-bit at the catalog's sample rate and long enough to be a sound, that none is silence, that rebuilding produces byte-identical output, and that the generator names no external source. `tests/test_audio_director.gd` proves the whole set loads, that a started harvest is heard and a restated one is not, that a reported hit is heard and a restated outcome is not, that an announced effect is heard, that nothing plays while sound is off, and that the toggle and the volume are applied, shown and written to the settings file. Both are wired into the workflow, and `check_provenance.py` passes. | IMPLEMENTED |
| 2.2 (c) Footsteps and declared ambience | The two halves of 2.2 that need no new opcode. A step is heard when the server says the player is standing on a different tile - the first sighting after login is not a step, and a burst of movement commands in one frame is one step, not four. Ambience comes from the package: `environment.ambientAudio` has been in the Four Gates manifest since it was authored, naming a civic murmur over the central plaza and falling water at every `Waterfall_` node with gains and a radius, and nothing read it. `AmbientAudioBinder` plays exactly what a package declares and invents no placement: an entry naming a `nodePrefix` becomes one positional player per matching node at the declared radius, an entry without one is played flat, and a loop this client has no sound for is skipped with a warning rather than substituted. Three more original sounds are generated for it. | `tests/test_ambient_audio.gd` proves both entry shapes bind, the declared gain and radius are the ones used, a positional player sits on the node the package named, a node outside the prefix gets nothing, ambience loops rather than playing once, a package declaring nothing gets nothing, an unknown loop is skipped, and the **shipped** Four Gates manifest still declares its two ambiences and binds. `tests/test_audio_director.gd` proves the first sighting is not a step, a tile change is heard, and a restated tile is not another step. `tests/test_native_sounds.py` covers the three new sounds like the rest. Both suites wired into the workflow. | IMPLEMENTED |
| 2.3 (b) Buff duration and other actors' buff icons | `SEND_BUFF_DURATION(96)` is unallocated on this server, so the only durations available are the ones `GET_ACTIVE_SPELL(44)` states, which the effect strip already counts down. `SEND_BUFFS(78)` - the per-actor mask - is decoded and reduced in 2.3, and marks a hastened target on the selection line; drawing it as an icon over each actor is outstanding. | Server: `tests/test_unimplemented_opcodes.py` pins 96. Client: the 78 decoding and its use are covered in `tests/test_protocol.gd` and `tests/test_world_input.gd`. | PARTIAL |
| 2.3 Active effects | The client decoded `GET_ACTIVE_SPELL(44)`, `GET_ACTIVE_SPELL_LIST(45)` and `REMOVE_ACTIVE_SPELL(46)` into `AppState.active_spells` and rendered none of it, so a player could not see any effect they were under. Two **server defects** were behind the empty state, both found by driving a real cast: `protocol.active_spell` referenced a `GET_ACTIVE_SPELL` constant that was never defined, so every buff-granting cast raised `NameError` inside the connection handler and killed that client's connection silently - no message, no further packets, nothing logged until shutdown; and nothing ever announced an expiry, because `has_buff` drops an expired effect the next time something asks. Both are fixed: the three constants are defined, and `World.expire_buffs` announces each expiry from a two-second sweep beside the existing idle sweep. Client: an effect strip naming each effect, drawing its icon and counting down to the moment the server stated. The buff ids are the server's; the names live in the client's spell catalog beside the spell names and sigil art already there, because no name is on the wire. `SEND_BUFFS(78)`, the per-actor visible-effect mask, also decodes now and marks a hastened target. | Client: `tests/test_protocol.gd` decodes an actor buff mask, the mask going empty, and rejects a truncated one. `tests/test_world_input.gd` proves the strip names and counts down two reported effects, fits 1280x720 clear of the resource rail, drops one the server removes, takes a resync list that states no durations, and does not show an effect whose stated time has already run out. Server: `tests/test_active_buffs.py` builds an active-effect frame (which could not be built at all before), clamps a long duration to the byte the packet has, and covers the sweep in four cases. End to end: `tests/integration/active_buffs_local.py`, 5/5 - it grants exactly the reagents the server names as missing, casts a real Shield, receives command 44 with buff id 0 for 90 seconds, and proves the connection survives the cast. Rendered: `active-buffs-before.png`, `active-buffs-after.png`, `active-buffs-removed.png`, human-inspected. | IMPLEMENTED |
| 2.4 No sigil is purchasable on the Eloria profile | **Content defect, not a client gap.** `World.buy_sigil` needs an NPC within three tiles that lists the sigil for sale, and the NPC roster format has no column for one: every record is built as `(actor, map_id, 0, ())`, price zero and sigils none. No character can therefore own a sigil, and `CAST_SPELL(39)` - the legacy sigil route the client's spell quickbar uses - always answers `spell_result(3)`, "you do not have these sigils". Confirmed on the wire in `spell_power_local.py`. The standardized `#cast` framework needs no sigils and works, which is how 2.3 and 2.4 are proved end to end. | `tests/test_spell_power_state.py::test_no_eloria_npc_sells_a_sigil_so_legacy_casting_is_unreachable` pins it; `spell_power_local.py` shows the refusal on the wire. No client change can correct it. | BLOCKED |
| 2.4 Spell power | Both the power a cast will use and the ceiling a character can reach are the server's - one is stored on the character, the other comes from its Magic level and nexus - and the only way to read either was `#sp`, which answers in chat text. A client could therefore only show a power control by parsing the chat stream or by keeping a second copy of the progression rules, and this client may do neither. Server change: `ELORIA_SPELL_POWER(231)` gated on the new `spell_power_v1` capability, restated on login, on a preference change, on a cast that sets one, and whenever a Magic level or nexus moves the ceiling; an effect that cannot be cast at all is left out rather than sent with a limit of zero. Client: the cast encoder takes the fork's optional trailing power byte (without it the frame is exactly the legacy one), a stepper bounded by the highest stated limit, each cast clamped to the limit stated for its own effect, and the power on every spell tooltip. | Client: `tests/test_protocol.gd` pins both cast frames byte for byte - with and without the power byte - decodes a two-effect state and rejects three malformed payloads. `tests/test_world_input.gd` proves the stepper offers nothing before the server states a limit, opens to the stated ceiling, stops at both ends, clamps each cast to its own effect's limit, and states the power on the tooltip. Server: `tests/test_spell_power_state.py` covers a full state, an out-of-reach effect left out, a stored preference reported back, one above the limit reported as the limit, and a client without the capability being sent nothing. End to end: `tests/integration/spell_power_local.py`, 8/8 against the real server. | IMPLEMENTED |
| 2.4 (b) Twelve quick slots and the sigils window | The quickbar had six spell slots for a 22-spell catalog, and the 64-bit sigil ownership set the server sends at login was reduced into `AppState.owned_sigils` and rendered nowhere - so "you do not have these sigils" on a failed cast was the end of the conversation. There are twelve slots now, ten of them shifted 1-0 and two left unbound for the rebinding UI in 3.3 rather than given a guessed chord, and a sigils window lists every sigil with what the server says the player owns, plus which catalogued spells that set reaches and what each of the rest still needs. Ownership stays the server's; the names are the client's spell catalog, where the spell names and buff labels already live, and nothing here decides whether a cast will succeed. | `tests/test_world_input.gd` proves there are twelve slots and twelve rebindable actions, that the window lists all 26 sigils with exactly the ones a real command 42 payload marked owned, that the summary counts them and names what each blocked spell still needs, that it fits 1280x720 clear of the resource rail, and that cancel closes it. | IMPLEMENTED |
| 2.5 Server-placed map markers | `SEND_MAP_MARKER(90)` and `REMOVE_MAP_MARKER(91)` were enum values the decoder never reached, so every marker the server placed - the waypoint set by `#waypoint`, the seven tutorial and quest markers `sync_tutorial_markers` maintains - arrived and was discarded. Both decode now and reduce into `AppState.map_markers`, keyed by the server's own marker id. A marker names its map as the server's file reference (`./maps/four_gates.elm`), so both sides of the comparison are reduced through one function rather than matching a path the client never uses. Markers for other maps are held, not discarded: the server never withdraws one because the player walked away, so the client does not invent that rule either. Rendering is split deliberately - a `MapMarker3D` pin on the map-camera layer for the minimap, and a 2D overlay plus a sidebar list for the full map, because the full map frames 1600 metres at once and a world-modelled pin is about one pixel there. Nothing decides a marker has been reached; only the server removes one. | `tests/test_protocol.gd` decodes a real marker and a removal, pins the map-reference reduction, and rejects three malformed payloads. `tests/test_world_input.gd` proves a marker for another map is held but drawn nowhere, that the pin sits on the server's tile on the map-camera layer, that the sidebar names the marker and its tile, that the overlay is handed this map's markers only, and that a removal takes the pin, the list entry and the overlay entry with it. End to end: `tests/integration/map_markers_local.py`, 3/3 against the real server - setting a waypoint places marker 490 with its tile, map and label, and clearing it removes that same id. Rendered: `map-marker-before.png`, `map-marker-full-map.png`, `map-marker-gameplay-view.png`, human-inspected. | IMPLEMENTED |
| 2.6 (a) Quest pinning - delivered, but not what 2.6 asked for | The quest journal (2.1) lists what the server sent, but a player following one quest had to keep the window open to see its progress. A Track button pins the selected quest to a HUD readout showing the title, the objective, the progress and the location. Which quest to watch is presentational selection - the player's choice about their own screen - and it is the only thing kept: everything shown is re-read from the server's journal on every command 224, so the readout follows a quest that becomes ready, and a quest the server stops listing stops being tracked rather than lingering. The readout is deliberately outside the cancel cascade: it is a HUD element the player pinned, not a window covering the screen. No map marker is invented for it either - markers are the server's (2.5). | `tests/test_extension_windows.gd` proves nothing is tracked until asked, that the readout states the quest, objective, progress and location, that it fits 1280x720 clear of the resource rail, that it follows a later journal that marks the quest ready rather than showing what was pinned, that a quest the server stops listing stops being tracked, and that it does not come back on its own when that quest returns. Rendered: `extension-quest-tracked.png`, human-inspected. **Scope correction:** this is not what work item 2.6 specified. 2.6 is the quest-identity protocol (below); this pinning HUD was built from the item title while the brief was out of context, and is recorded here under its own heading rather than left standing as 2.6. | IMPLEMENTED |
| 2.6 (b) Quest tracking, as specified | **Not on the wire.** 2.6 is `NEXT_NPC_MESSAGE_IS_QUEST(92)`, `HERE_IS_QUEST_ID(93)`, `QUEST_FINISHED(94)` and the `WHAT_QUEST_IS_THIS_ID(63)` request - the protocol that populates the journal from dialogue. This server allocates none of those four numbers in the server-to-client direction and references none of the names anywhere. The journal itself is populated instead by command 224, which is implemented and proved in 2.1. | Server: `tests/test_unimplemented_opcodes.py` pins all four as unallocated and unreferenced; defining any of them fails it. | BLOCKED |
| 2.7 ITEM_ON_ITEM(42) and DO_EMOTE(70) | **Nothing behind either command.** Neither number is defined as a client command in `eloria/protocol.py`, neither has a dispatch branch in `eloria/server.py`, and neither has a concept in the world model: there is no item-on-item combination anywhere, and no emote catalog, emote state or emote broadcast - `grep -rn -i emote eloria/` matches only `remote_heal`. Adding an encoder for either would put bytes on the wire that nothing reads, which is exactly the "no stubs" rule. Building either means designing new gameplay and new animation content, which is outside closing a client feature gap. | `tests/test_ground_item_inspection.py::test_the_other_two_legacy_commands_have_nothing_behind_them` pins the absence, so building either concept fails that test and forces this row to be replaced with a proof. | BLOCKED |
| 2.7 Looking at an item on the ground | `LOOK_AT_GROUND_ITEM(24)` had no encoder in the client and no handler in the server. The ground-bag packet carries an image id, a quantity and a slot, so a player could see a picture of something lying on the ground and had no way to learn what it was. Server change: `World.inspect_ground_item` answers through the same two routes an inventory item does - the fork's item-detail packet for a client with `item_detail_v1`, the legacy description for any other. Client: the encoder and a Look action in the ground-bag window, enabled only with a row selected. | Client: `tests/test_protocol.gd` pins the encoder bytes; `tests/test_world_input.gd` proves the action needs a selection first and becomes available with one. Server: `tests/test_ground_item_inspection.py` covers the capable reply, the legacy reply, an empty slot, a slot outside the bag and no bag open. End to end: `tests/integration/ground_item_local.py`, 9/9 - a real drop, the bag the server made, and a description that agrees with the bag row's own image and quantity, plus a second character advertising nothing getting the legacy packet for its own bag. | IMPLEMENTED |
| 2.8 (c) Chat conveniences | The brief scopes ignore and filter lists, channel colour configuration, chat tab-completion, URL detection and PM/AFK logging into 2.8 alongside the buddy list. None needs a server feature. Outstanding, and carried into the Phase 3 corrective pass. | Not yet built; recorded so the gap is visible rather than implied by the guild-tag row above. | NOT_STARTED |
| 2.8 Buddy list | **No such concept on this server.** No buddy or friend table, no add or remove command, no online notification, nothing on the wire. Guilds are the social structure this server has, and the guild tag above is what a client can show for it. Building a buddy list means designing the feature server-side first. | `tests/test_guild_tag_wire.py::test_there_is_no_buddy_list_to_render` pins the absence across `server.py`, `world.py` and `protocol.py`, so building one fails that test and forces this row to be replaced with a proof. | BLOCKED |
| 2.8 Guild creation is out of reach for a test character | Noted while trying to prove the tag end to end. `#make_guild` needs 30,000 Gold Coins in the backpack, and gold weighs 1 EMU against a carry capacity of `20 x might`, which the `#boost` command caps at 20,000 EMU with both attributes at their maximum of 1,000. No character can hold the fee, so the tag path cannot be driven from a probe. The encoding is instead pinned on both sides against the server's own builder. | The arithmetic above, from `eloria/guilds.py` (`GUILD_CREATE_COST`), `eloria/models.py` (`carry_capacity`, `might`) and `eloria/stats.py` (`boost_stat` attribute range). | BLOCKED |
| 2.8 Guild tags on players | The server builds an actor's display name as one string - an optional name-colour byte, the name, a space, an optional tag-colour byte and the guild tag - and the client took the whole thing as the name. A player in a guild therefore rendered with the colour byte as mojibake and the tag glued to their name. `decode_actor_name` splits all four parts, the reducer keeps them apart, and the nameplate draws the tag as a tag. Both colours are the server's choice and are decoded rather than dropped; neither is ever chosen client-side. | Client: `tests/test_protocol.gd` decodes a tagged actor, an untagged one and a coloured name, from byte-exact output of the server's own `enhanced_actor_packet`. `tests/test_world_input.gd` proves the reducer keeps the name and the tag apart and that the nameplate draws `Alice  [ELO]`. Server: `tests/test_guild_tag_wire.py` pins the encoding in five cases - tag appended after a space, viewer-specific tag colour, name colour, no trailing space without a guild, and both colours at once. | IMPLEMENTED |
| 2.9 Dynamic world objects, as specified | **Not on the wire.** 2.9 is `GET_3D_OBJ_LIST(74)`, `GET_3D_OBJ(75)`, `REMOVE_3D_OBJ(76)`, `MAP_SET_OBJECTS(220)` and `MAP_STATE_OBJECTS(221)`, plus teleporters (10, 12, 13) and mines (80, 81, 82). The server allocates none of those numbers server-to-client and has no concept behind any of them, so every map stays at its manifest state and no client change can alter that. The map-object layer that does exist - which object ids are on the map, where, and what they require - is `ELORIA_MAP_OBJECTS(236)`, implemented in 1.3. | Server: `tests/test_unimplemented_opcodes.py` pins all eleven. | BLOCKED |
| 2.10 (b) Achievements window | `SEND_ACHIEVEMENTS(95)` is allocated and sent, but only in answer to `GET_PLAYER_INFO` about **another** player, and this server's whole catalog is one achievement ("Beginner Tutorial", id 0). The fork's `ELORIA_PLAYER_INFO(228)` supersedes the bitset with names, and 2.10 renders it. A dedicated window listing up to 1024 achievements has nothing to list and no packet that would fill it for the player's own character. | Server: `eloria/world.py` sets `native_ids = {0} if "Beginner Tutorial" in target.achievements`; `tests/test_player_info.py` covers both reply paths. | BLOCKED |
| 2.10 Combat mode | `COMBAT_MODE(8)` is defined on both sides and sent by nothing: `grep -rn COMBAT_MODE eloria/` finds the constant in `protocol.py` and no call site anywhere in the server. Decoding it would be dead code. It is also superseded rather than missing: the client already learns combat state from two things the server does send - the actor frame, consumed in 0.9, which says whether an actor arrives already in combat, and command 227, which states both health bars and the outcome of each exchange and is proved in 2.1. | The grep above; `tests/test_protocol.gd` (0.9) covers the actor-frame combat state and (2.1) the combat-state packet. | BLOCKED |
| 2.10 Looking at a player, and achievements | The client had no encoder for `GET_PLAYER_INFO(5)` and no decoder for `SEND_ACHIEVEMENTS(95)`, so another player could not be inspected at all. The legacy reply is also unreadable on its own - a "You see: `<name>`" chat line plus a bare 160-bit set with no actor id and no names - so reading it means pairing the bitset with a request the client remembers making and keeping a second copy of the server's achievement catalog. Both are the client inferring what the server can state. Server change: `ELORIA_PLAYER_INFO(228)`, gated on the new `player_info_v1` capability, states the actor id, the name and the achievements by name; a client without the capability gets exactly what it got before. Client: the `look_at_player` encoder, a Look action beside Attack and Trade, and a player-info window that renders the reply and nothing else. | Client: `tests/test_protocol.gd` pins the encoder bytes, decodes a reply with and without an achievement, and rejects three malformed payloads. `tests/test_world_input.gd` proves the window names the player and the achievement, fits 1280x720 clear of the resource rail, joins the cancel cascade, and describes a player with nothing earned rather than going stale. Server: `tests/test_player_info.py` covers the capable reply, the empty case, the legacy pair with bit 0 set, and a player on another map not being described at all. End to end: `tests/integration/player_info_local.py`, 5/5 against the real server with three logged-in characters - the capable session gets 228 naming the actor it asked about and no chat line to parse, the plain session still gets `RAW_TEXT` and 95. The earned-achievement case is proved by the server test against the real builder, not end to end: granting one requires walking the whole beginner tutorial. Rendered: `extension-player-info.png`, human-inspected. | IMPLEMENTED |
| 3.1 (a) World effects - delivered early, under the wrong item number | `SEND_SPECIAL_EFFECT(79)` had no decoder, so a swarm of bees interrupting a harvest, a lucky find, and every spell landing all happened with nothing on screen. It decodes now and is announced as an event rather than stored as state - there is nothing to still be true a moment later - and `WorldEffect3D` draws a generated burst at the actor the server named, with a beam when the server named a second one. The visual is deliberately abstract: the effect ids are a legacy namespace with no names on the wire, and inventing a distinct piece of art per id would be inventing meaning the server never sent. An effect at an actor the client has not been told about is not drawn rather than placed at a guess. | `tests/test_protocol.gd` decodes an effect at one actor and one between two, and rejects three malformed payloads. `tests/test_world_input.gd` proves the effect is drawn where the actor stands, that no beam appears without a second actor, and that an unknown actor produces nothing. End to end: `tests/integration/special_effects_local.py`, 5/5 - it harvests a real level-0 resource on the real map until the server's own one-in-125 harvest event fires, and reads effect 17 for the harvesting actor after 69 inventory updates. Rendered: `world-effect-before.png`, `world-effect-at-actor.png`, `world-effect-between-actors.png`, human-inspected. **Scope correction:** `SEND_SPECIAL_EFFECT(79)` belongs to work item 3.1, not 2.9. It was built under 2.9 from the item title while the brief was out of context. The work stands; the row is renumbered. | IMPLEMENTED |
| HUD finding: the quickbar and the resource rail overlap by five pixels | Pre-existing and unrelated to this phase's work. Measured at 1280x720: the item/spell quickbar occupies y 62-379 in the same right-hand column as the fixed resource rail at y 374-564, so their rectangles overlap by five pixels of panel margin. Confirmed at `bbeb5397~1`, before any Phase 2 change to that panel: the quickbar was `[1198, 62, 74x317]` and already intersected. Nothing of the rail's content is obscured in any rendered frame, so this is recorded rather than fixed inside a feature commit. | The measurements above, taken from `main.tscn` at both revisions. | OPEN |
| Phase 0 open finding closed: the startup `Parse JSON failed` warning | Recorded in Phase 0 as an untraced warning at client startup. It is not a startup warning and not a defect: `tests/test_protocol.gd` feeds command 233 a deliberately malformed payload (`{`) to prove the invasion-assistant decoder rejects it, and Godot logs the parse failure from inside `JSON.parse_string`. Running the client alone (`--quit-after 3`) logs nothing of the kind. | `godot --headless --path godot-client --quit-after 3` produces no `Parse JSON` line; the message appears only in the protocol suite, whose rejection case asserts `type == "invalid"`. | RESOLVED |

## Phase 3 (P2)

| Item | Finding / change | Test/evidence | Status |
|---|---|---|---|
| 3.1 (b) Particles | There was no particle system of any kind. The world effect delivered under 2.9 drew a ring and a beam; it is now a real `GPUParticles3D` burst - a soft generated radial dot, additively blended and billboarded, rising from the actor with a ground ring and a beam when the server named a second one. The effect ids are a legacy namespace with no names on the wire, so nothing invents a distinct piece of art per id: they are sorted into the three classes the server actually uses, and a harmful effect falls while a beneficial one rises, which is the only distinction its own grouping supports. Everything is built from primitives and generated textures; nothing is imported. Confirmed rendering under `--rendering-method gl_compatibility`, which is the client's declared renderer. | `tests/test_world_input.gd` proves the burst exists and is configured, that the two classes differ in both movement and colour rather than sharing one burst, and that an effect at an unknown actor is still drawn nowhere. Rendered: `world-effect-at-actor.png` and `world-effect-between-actors.png` re-captured with the particles, human-inspected. | IMPLEMENTED |
| 3.1 (c) Weather and world emitters | **Not on the wire.** `SEND_WEATHER(100)`, `START_RAIN(15)`, `STOP_RAIN(16)`, `THUNDER(17)`, `FIRE_PARTICLES(61)` and `REMOVE_FIRE_AT(62)` are unallocated in the server-to-client direction and referenced nowhere in the server, so there is no weather to render and no emitter to place. The particle work above is the part of 3.1 that has a packet behind it. | Server: `tests/test_unimplemented_opcodes.py` pins all six. | BLOCKED |
| 3.2 Day and night | `NEW_MINUTE(5)` arrived every real minute and moved the clock face and the compass and nothing else: the sun, the ambient light and the fog came from the map manifest and never changed, so every map sat permanently at whatever hour it was authored for. The hour now drives the environment. The daylight curve is **the server's own** - `World` computes `(1 - cos(2*PI*minute/360)) / 2` to decide how far a creature can see, minute 180 brightest and minute 0 darkest - so what the player sees and what the server acts on cannot drift apart. The manifest stays the reference: what a package declares is full daylight, and the binder dims, warms and rotates from there, returning to exactly the declared values at noon so repeated application cannot drift. A package opts out with `environment.dayNight: {"enabled": false}`, and an interior that declares no sun is left alone - its lamps are its whole lighting. The client also carries the clock forward between packets using the interval it **measures** between two of them rather than a hard-coded tick, so the sky moves instead of stepping once a minute. | `tests/test_day_night.gd` checks the curve against the server's expression at six minutes, that 180 and 0 are the extremes, that twilight peaks at sunrise and sunset, that an outdoor package is driven while an interior and an opted-out package are not, that midnight is a fraction of noon with ambient dimmed but never black and no shadow from a set sun, that sunrise is warmer than noon, that returning to noon restores the declared values exactly, and that the carried clock reports the stated minute until two have been seen. Rendered: `day-night-before.png` plus noon, sunrise, sunset and midnight, with the run asserting noon > sunrise > midnight by measured luminance - human-inspected. | IMPLEMENTED |
| 3.3 Settings and rebindable keys | The client had one flat panel with four controls and 37 input actions that could not be rebound at all - the brief notes this was a prerequisite left over from 0.7. A tabbed window carries Graphics (shadows, effects, nameplates), Camera (rotation and pan sensitivity, follow), Gameplay and Controls, with the four existing controls staying in the small panel that already held them. Every binding in the table is a real declared action, so no row can be dead, and while the window is waiting for a key every key press belongs to it - otherwise rebinding "attack" would attack. Bindings persist as text and a value that cannot be read back **leaves the action alone**: erasing first and failing to parse would silently unbind it, which is exactly how a stale settings file took the map key away during this work. The Gameplay tab sends the server's own commands (`#targetmode`, `#autogather`) rather than deciding anything locally. **Dropped deliberately from the legacy 296:** every fixed-function OpenGL toggle (shadow mapping mode, texture compression, mipmap bias, point/particle size, fog mode, anti-aliasing selection, poor-man's shadows, clouds, reflections), the font and text-shadow tabs, the server-selection tab, the troubleshooting and debug tabs, and the audio device selection - none of them means anything to a Godot GL-Compatibility client that gets its rendering choices from the project's own renderer settings. | `tests/test_settings_window.gd` proves the window opens tabbed with four sections and fits 1280x720 clear of the resource rail, that every bindable action exists, that a captured key rebinds the action with its modifiers and is stored as readable text, that Escape keeps the old binding, that a stored set is read back exactly, that an action which no longer exists is skipped, that an unreadable stored value leaves the action as it was, that turning effects off stops them being drawn and back on restores them, that a camera slider reaches the camera, and that cancel closes the window. | IMPLEMENTED |
| 3.4 Slash-command console | The console showed text and had no command table, so every `#` command went out as raw text - including the ones no server has ever implemented, which came back as "unknown command" or silence. `ConsoleCommands` answers the eighteen that are about the player's own screen: marks, ignore and filter lists, aliases, an away message, arithmetic, a search over what they have been told, and a help listing. Anything not in the table reaches the server exactly as typed, because deciding locally what the server should have answered is the mistake this codebase avoids. `#calc` refuses anything that is not digits, spaces, brackets and operators before it goes near `Expression`, which would otherwise run arbitrary GDScript. Ignore and filter decide only what the player is **shown**: the line still arrived, is still in state, and the console tab still shows everything. Up and down walk the history; Tab completes as far as the local commands agree and lists the rest. Marks, ignores, filters and aliases persist in the client's settings file. **This also closes 2.5's user markers and 2.8's chat conveniences**, which the brief scopes into this command table. | `tests/test_console_commands.gd` proves five non-commands are left for the server, that a mark records the tile the client was told it is on, that re-marking a label moves it rather than duplicating, that `#markpos` takes an explicit tile and says how to use it when malformed, that ignore and filter are case-insensitive and reversible, that an alias expands only its own head and never eats a message, that `#calc` computes and refuses non-arithmetic, that `#find` searches what the client was told, that completion offers every command with a prefix, and that `#help` lists the whole table. `tests/test_world_input.gd` drives the real chat submit path: a local command writes its reply and clears the box, a mark reaches the map overlay, Tab completes to the shared prefix, and up and down walk the history. | IMPLEMENTED |
| 3.6 Ranging and missiles | `MISSILE_AIM_A_AT_B(84)` and `MISSILE_FIRE_A_TO_B(86)` were undecoded, so a ranged fight was two actors standing still while damage numbers appeared - nothing was ever drawn between them. Both decode now. Aiming is state and lives on the actor it describes, so it disappears with them; loosing is an event, ends the aim the server stated before it, and draws an arrow from where the shooter stood to where the target stood. The shot is already resolved when the packet arrives - the damage comes separately - so nothing here decides anything. The arrow is drawn thicker than life with a trail, because a true-scale arrow is two pixels at gameplay camera distance, which is no better than drawing nothing. The selection line reports who the server says the player is aiming at. | Client: `tests/test_protocol.gd` decodes an aim and a shot from the server's own builder output and rejects two malformed payloads. `tests/test_world_input.gd` proves an aim is kept on the actor and draws nothing, that loosing draws an arrow between the two and ends the aim, and that a shot at an unknown actor draws nothing. End to end: `tests/integration/ranging_local.py`, 6/6 against the real server - it equips the shipped Hunting Bow and Arrows, walks far enough from a real Reedhorn Stag for the server to allow a shot, attacks, and reads six aim/shot pairs naming the same shooter and target. Rendered: `world-effect-missile.png`, human-inspected. | IMPLEMENTED |
| 3.6 Point-target missiles and the outbound fire command | **Not on the wire.** `MISSILE_AIM_A_AT_XYZ(85)`, `MISSILE_FIRE_A_TO_XYZ(87)`, `MISSILE_FIRE_XYZ_TO_B(88)` and the outbound `FIRE_MISSILE_AT_OBJECT(51)` are unallocated: this server only ever shoots from one actor at another, and ranging is started by the ordinary attack command with a bow equipped. | Server: `tests/test_unimplemented_opcodes.py` pins all four. | BLOCKED |
| 3.7 Localization and screenshots | Every string in the client was hardcoded, and there was no screenshot binding at all. The translation system is adopted now rather than retrofitted later: `data/i18n/strings.csv` is imported by Godot into a `.translation` - the CSV is the source and the generated file is left out of the repository like every other import artefact, so the workflow's existing import step produces it - the project loads it, and the first slice of windows reads through `tr()` - the settings window, the sigils window, the player-info window and the quest journal's two fixed lines, 28 strings in all. **Everything else in the client is still hardcoded**, which is stated here rather than implied: this pass adopts the mechanism and converts a coherent slice, and each later window converts as it is touched. A screenshot goes to the user directory under a timestamped name, because a client may be installed somewhere it cannot write, and the player is told the path. | `tests/test_localization.gd` proves the CSV imports as a `Translation`, that every key in it has an English string, that the project registers it so `tr()` needs no setup, that every key the converted windows ask for exists - so a player can never be shown a raw key - that `tr()` resolves through the running `TranslationServer`, that an unknown key comes back unchanged, that a placeholder string formats, and that with no framebuffer the client says the capture failed rather than reporting a file it did not write. `tests/integration/rendered_screenshot.gd` proves the capture under a real display: the file is where the client said, it is a full 1280x720 image, and the player is told the path. Both wired into the workflow. | IMPLEMENTED |
| 3.5 Reference windows | The legacy client had ten file-driven reference windows and this client had none. Four are built, chosen because they can be built honestly. **Help** is generated from the client's own input map and console table, so it cannot drift from what the keys and commands actually are - rebinding a key changes the help with nobody editing it. **Notes** is the player's own notepad, kept in the client's settings file and never sent anywhere. **Links** collects the addresses the server has said in chat, each one once, reading what arrived and inventing nothing; the detector lives beside the ignore and filter lists, which is the other half of 2.8. **Encyclopedia** is laid out the way the legacy client lays its own out - an index of categories, a page of links per category with the long ones paged, and a page per entry with a Back To Index link at the foot of it - plus Back, search and bookmarks. Half of it is original Eloria reference text describing this server's own rules, and half is built from the catalogues the client already loads to play the game: every recipe, spell, book, region and skill has a page, and a recipe page states that recipe's own materials, tools, book, level, experience and food. It is reached from its own lower-HUD icon and from Ctrl+E as well as from the tab. | `tests/test_reference_window.gd` proves the four pages exist and fit 1280x720 clear of the resource rail, that help lists a bound key and every console command, that rebinding a key changes the help, that an address said twice is listed once while a line without one adds nothing, that a bare `www.` address is found without its trailing comma, that a note reaches the settings file, that the encyclopedia has real entries and shows them, and that cancel closes the window. `tests/test_encyclopedia.gd` proves the HUD icon and Ctrl+E open it on its own page while a bare E does not, that every alchemy page states its recipe's own numbers and materials, that a recipe links to the book it needs and the book links back, that a long category is paged, that Back walks the trail, that search reads both halves, and that a bookmark reaches the settings file and no further. The same suite proves the move icon shows the attack icon while Alt is held and goes back when it is let go, and that an attack mode chosen from the HUD survives both. It also restores the key it moved and saves, because these suites share one settings file. | IMPLEMENTED |
| 3.5 Skills, rules and astrology | **Not built, for three different reasons.** Skills already have a window - the statistics panel shows every skill and its experience - so a second one would be a duplicate. Rules and astrology are server policy: this server has a full special-day catalogue and a game date, and states both **only as chat text** (`GET_DATE` answers with a coloured line, and a special day is announced in chat). A client window would have to parse the chat stream, which this programme forbids. **Server change needed to unblock:** state the current special day, its effects and the game date as a small extension packet, the way `ELORIA_PLAYER_INFO(228)` and `ELORIA_SPELL_POWER(231)` already do; the client side is then a page in the window that exists. | The chat-only paths are `eloria/protocol.py::game_date` and the special-day announcements in `eloria/world.py`. | BLOCKED |
| 3.8 Actor animation commands | **Not on the wire.** `ADD_ACTOR_ANIMATION(89)` is unallocated in this server and referenced nowhere, so there is no arbitrary animation for a client to play. The animations that do arrive - idle, walk, combat, sit and stand - come as actor commands and are already driven, back to Phase 0's frame handling. | Server: `tests/test_unimplemented_opcodes.py` pins 89. | BLOCKED |

### Verified rendered evidence: the Eloria extension windows

`extension-windows-before.png` is the HUD with the same client before any
extension packet arrives: none of these windows existed, so the packets that
drive them changed nothing on screen. The six after-frames are the same HUD
once each packet is decoded through the client's own `decode_server()` and
reduced into state; every payload is byte-for-byte what the server's builder
in `eloria/protocol.py` emits.

Human inspection of the frames: `extension-hud-readouts.png` shows the
waypoint line naming "Reed bank" at tile 770, 481 twelve tiles away, the
combat panel with both health bars and "Hit for 5" under the target name, and
the special-event panel carrying both server lines - none of which overlap the
fixed resource rail. `extension-quest-journal.png` shows one quest at 1/3 with
its objective and location; `extension-mail.png` shows the unread marker and
the selected body; `extension-item-detail.png` shows the description, the EMU
line and the comparison against the equipped Guard Cape;
`extension-merchant.png` shows the merchant window with the NPC, the purse,
the load, the buy price and the quantity ladder, and nothing of the NPC
dialogue it replaces; `extension-marketplace.png` shows gold, escrowed items
and one live listing with its seller.

## Inventory window refinements (post-Phase 3, player-requested)

These came from playing the client rather than from the brief's item list, and
are recorded here on the same terms as everything else.

| Item | Finding / change | Test/evidence | Status |
|---|---|---|---|
| Slots always drawn | The backpack and wear grids only appeared once something was selected or carried: empty slots were `disabled`, and Godot draws a disabled button flat, with no frame. The window therefore had no shape until it was in use. Empty slots are enabled now; pressing one with nothing to place was already a no-op, so nothing else changes. | `tests/test_world_input.gd` proves both grids are drawn with nothing selected and still carry the "Empty ..." tooltips. Rendered: `inventory-slots-idle.png`, human-inspected. | IMPLEMENTED |
| One tool at a time | A left click meant "grab" in walk mode and "select and inspect" everywhere else, the four buttons each acted immediately, and the detail window opened on every click. There is now one tool in hand - Move, Use or Inspect - which a right click on any item steps through, and which the Use, Equip, Unequip and Inspect buttons each select and apply at once, so the buttons and the click are the same mechanism under the same names. The description line along the bottom names the tool in hand. | `tests/test_world_input.gd` proves the right click steps grab -> use -> inspect -> grab with the buttons showing which is held, and that an action button takes its tool. Rendered: `inventory-move-short-line.png` and `inventory-inspect-card.png`. | IMPLEMENTED |
| Short line, and the card only when asked | Every look-at opened the detail window over the inventory. The short line and the card are two readings of the same `ELORIA_ITEM_DETAIL(225)` reply, so which one the player gets is decided in the client before the request rather than by asking the server twice: every tool writes the line, and only Inspect lets the window open. Nothing is inferred - the line reports the name, category, quantity and equipped flag that arrived, and nothing when the reply carries none of them. | `tests/test_world_input.gd` proves a move click writes the line with the card shut, and that Inspect opens the card and still writes the line. Rendered: the same two frames, human-inspected. | IMPLEMENTED |
| Seven-digit quantities | The quantity boxes took five digits and the count drawn over a slot had a fixed 31-pixel box, so a large stack was cut off part way through the number. Both take seven now: the box's limit and the clamp share one constant, and the overlaid count runs the width of the slot and steps its size down as the number grows. An eighth digit does not fit in the box rather than being taken and silently altered. | `tests/test_world_input.gd` proves the box takes seven digits, keeps a seven-digit value whole, refuses an eighth, and that a 1,234,567 stack is drawn whole over its slot. Rendered: `inventory-move-short-line.png` shows the stack in the slot and in the line. | IMPLEMENTED |
| Bottom of the window | The window was 40 pixels shorter than the rows it held, so with the slots always drawn the load line and the quantity boxes were pushed past its bottom edge. The window is tall enough for its contents and the body takes the slack, which puts both rows on the bottom edge at any size. | `tests/test_world_input.gd` proves the window still fits a 1280x720 screen. Rendered: `inventory-slots-idle.png`. | IMPLEMENTED |
| The bag window resizes | The inventory had a drag grip and the bag had none, so a bag could not be made bigger to read or smaller to get out of the way. The bag has the same grip, its own scale and its own line in the settings file, and the clamp that already kept it on screen while being dragged now accounts for that scale. | `tests/test_world_input.gd` proves the grip exists, that dragging it scales the bag and leaves the inventory alone, that it cannot be shrunk past legibility, and that both windows stay on a 1280x720 screen. Rendered: `bag-window-default.png` and `bag-window-resized.png`, human-inspected. | IMPLEMENTED |

Rendered by `tests/integration/rendered_inventory_tools.gd` under
`--rendering-method gl_compatibility`, which is the client's declared renderer.

## Rendered development-server evidence gate

`tests/integration/rendered_server_session.gd` drives the real login and character-creation UI against the authorized development endpoint from an opt-in Xvfb GitHub Actions job. It generates a disposable name and strong password in memory, never prints either value, and records credentials only as `REDACTED`.

The gate fails unless it proves all of the following before uploading evidence:

- the server supplied a map ID, local actor ID, spawn DTO, and authoritative tile;
- the local actor has a visible native luminous GLB with at least three meshes and no missing-model fallback;
- a navigation-surface ray exists below the spawn and the actor foot agrees with it;
- the actor is in front of and within the gameplay camera frame;
- right-drag rotation, middle-drag pan, and wheel zoom change their respective
  camera state while the actor remains in frame, then restore actor follow;
- the white player marker follows the actor and both map cameras include its layer;
- a left-click routed through the gameplay viewport produces a server actor-tile update;
- a second real connection creates and logs in a disposable player without
  persisting credentials, and the primary client receives its authoritative
  spawn, movement, and disconnect;
- the remote player uses a visible native luminous model, can be selected by a
  world-space ray, and is removed from both state and presentation after its
  connection closes;
- chat submitted through the primary lower-left UI reaches the second real
  client, and chat sent by the second client is decoded and presented by the
  primary reducer/UI;
- the authoritative inventory and statistics snapshots are non-empty, the
  inventory and statistics windows fit within 1280x720 without covering the
  fixed resource rail, and every item has a native or explicitly disclosed
  independent-Eloria substitute icon plus its quantity; character values, item
  quick slots, and all configured spell quick slots are presented from that
  state;
- selecting every real backpack item sends `LOOK_AT_INVENTORY_ITEM`, receives
  its authoritative description, and presents the selected description in the
  inventory window;
- equipping the real guard spear moves it into wear slot 36, applies the
  server-broadcast actor visual to the native skeleton through a visible
  development fallback, and unequipping restores inventory and clears it;
- each 1280×720 capture contains rendered color variation rather than a dummy frame.

The artifact contains default, rotated, panned, zoomed, full-map, and
post-movement PNGs, selected-remote-player, chat, inventory, and statistics
PNGs plus an equipped-fallback PNG, and sanitized session, camera-state,
movement, remote-actor, chat, inventory/statistics, and equipment JSON. A
passing structural assertion is still classified separately from human visual
inspection of those PNGs.

### Verified rendered evidence: Phase 0 marker lights and diagnostics panel

`tests/integration/rendered_phase0_repairs.gd` produces the two before/after
pairs whose only meaningful proof is a capture, and runs as its own opt-in-free
`rendered-phase0-repairs` CI job at 1280x720 under Xvfb. It needs no server: it
drives the client's own world loader, environment binder and light-marker
binder, and the client's own reducer for the diagnostics packets. Every frame
is checked for real colour variation, so a dummy frame cannot pass.

- `marker-lights-before.png` / `marker-lights-after.png` - the Sunmane Wind
  Caves drovers' camp with the marker binder skipped and with it applied. The
  manifest declares brazier markers and they are the interior's entire light
  rig. Human inspection of both frames: the "before" frame is the same
  geometry in near-darkness, lit only by the manifest's ambient term, with the
  timber ceiling, the stalactites and the far side of the chamber unreadable.
  The "after" frame has warm brazier fill throughout, the timber gallery and
  ceiling beams are legible, the stalactites are separated from the rock behind
  them, and the far anvil and cook-pot read as objects. Mean sampled luminance
  rises from `0.1438` to `0.2133`. This is the difference between the shipped
  client and the fixed one for every interior in the game.
- `reading-progress.png` / `reading-complete.png` - the reading window, which
  had no equivalent before. Human inspection: the panel sits top-right clear of
  the resource rail and reads "Reading Metallurgy", a quarter-filled progress
  bar and "150 of 600 pages read (25%)". Note the title: index 0 is
  `Metallurgy` in the client's catalog and `Beginnings` on the Eloria server,
  which is the catalog mismatch recorded above, visible in the capture.
- `harvest-targets-idle.png` / `harvest-targets-active.png` - the world-object
  pick layer, which the client had no equivalent of at all. Human inspection:
  three rings stand on the tiles the server named - two green harvest nodes and
  one blue interactive - and in the active frame the node the server reports
  harvesting has turned gold while the lower-centre indicator reads
  "Harvesting Sunleaf", clear of the chat entry, the lower action rail and the
  right-hand resource rail.
- `server-popup.png` - the server's summon-behaviour popup, decoded from the
  exact bytes `protocol.summon_behavior_popup()` produces. There is no "before"
  half because the server had no way to ask a question at all. Human
  inspection: the title, the body line, all six behaviour options in the
  server's order with the server's labels, and the Send/Dismiss row, centred
  and clear of both the right-hand resource rail and the lower action rail.
- `console-history.png` / `console-diagnostics.png` - the console panel showing
  its session message history, then the same panel after the `Protocol` toggle
  with one opcode the client does not decode delivered twice and one pre-0.6
  single-byte trade acceptance. Human inspection of both frames: the
  diagnostics view reads `199  x2  last payload 1 bytes, 0s ago`, `total
  undecoded packets: 2`, `36  trade_accept_length  (1 bytes, 0s ago)`, and the
  connection block. Both frames sit inside 1280x720, clear of the right-hand
  resource rail, the clock and the compass, with the lower action rail intact.

Both pairs were rendered and inspected on the development machine at
1280x720 through the GL Compatibility renderer before the CI job existed; the
CI job reproduces them so the evidence is regenerated on every change.

### Verified evidence: workflow run 33068336019

The opt-in rendered job passed against the development server on commit
`52f365a08b6f4ec274f165d107f6185f38532a7c`. Its uploaded artifact is
`rendered-server-session` (artifact ID `9644708960`). Human inspection of the
1280×720 captures confirmed the native local actor on the rendered terrain,
the lower/right HUD rails, a populated unobscured minimap, a visible white
player marker on the minimap and Tab map, and continuous camera follow after
movement.

Sanitized runtime values:

- server map ID: `four_gates`;
- local actor ID: `120`;
- authoritative spawn tile: `(768,480)`;
- converted/final actor position: `(178.8372,34.14781,-44.88372)`;
- navigation hit: `(178.8372,34.12781,-44.88372)` on
  `Terrain_City_Plateau_WalkSurfaceCollision`;
- native model: three visible meshes, native glTF hierarchy retained, no
  development fallback;
- camera: pitch `-60` degrees, distance `26`, FOV `50`, actor projected height
  `28.0842` pixels;
- MOVE_TO redacted-safe bytes: `01 05 00 05 03 e1 01`, target tile `(773,481)`;
- authoritative result: local actor reached `(773,481)`, faced movement at yaw
  `-0.785398`, and remained the camera focus.

The job is commit-message opt-in (`[rendered-integration]`) because every run
creates a disposable server character. Ordinary pull-request updates only run
the deterministic headless suite.

### Verified camera-state evidence: workflow run 33068947381

Commit `2ee71b536e525120cb1237334ce390c721cbbd8a` passed both the
headless protocol job and the opt-in real-server rendered job. Artifact
`9645038330` adds human-inspected rotated, panned, and zoomed 1280×720 frames
plus sanitized `camera-states.json`:

- rotation: yaw `0 -> -30` degrees and pitch `-60 -> -56` degrees;
- intentional pan: `(0,0,0) -> (5.799599,0,1.186798)`;
- zoom: distance `26 -> 18.5`;
- the native actor remained inside the gameplay camera in every state;
- restoring the default camera reset pan, re-established actor focus, and the
  subsequent real MOVE_TO still reconciled `(768,480) -> (773,481)` with actor
  yaw `-0.785398`.

All camera-state PNGs were visually inspected. Rotation, pan, zoom, the HUD,
the populated minimap, and the white minimap marker rendered in every frame;
the zoomed frame made the native actor substantially easier to read.

### Verified sit/stand evidence: workflow run 33069841631

Commit `f677c789fa05f51ae8f25657c5e7872b5cee5724` passed both jobs.
Artifact `9645336657` records local actor ID `122` and contains visually
inspected seated and standing-after-move frames. The real server accepted
desired-state packets `07 02 00 01` (sit) and `07 02 00 00` (stand), then
broadcast commands 13 and 14 through the normal actor reducer.

The native animation now progresses from `Sitting_Enter` to the explicit
`seated_idle` action and from `Sitting_Exit` back to `idle`. A second sit
followed by MOVE_TO proved server-driven automatic standing: the actor moved
from `(773,481)` to `(774,481)`, finished in `walk`, and did not retain a stale
seated pose. Credentials remained `REDACTED` in every artifact file.

### Verified remote-player evidence: workflow run 33073030852

Commit `7b5b6b1bbfa376d879d4966a1286c7882edde5ce` passed the strict
headless job and the opt-in rendered development-server job. Artifact
`9646673225` contains the sanitized remote actor diagnostics and the visually
inspected 1280×720 selected-player frame.

The helper connection created and logged in a second disposable character with
credentials retained only in memory. The primary client received remote actor
ID `124` as enhanced player kind `1`, preserved the native luminous glTF model
with three visible native meshes and no missing-model fallback, applied the
authoritative movement from `(767,479)` to `(763,479)`, and selected the remote
collision capsule through the gameplay camera ray. The selected actor's gold
ring and both native players are visible on the authored terrain in the capture.
Closing the helper connection removed actor `124` from both authoritative state
and the rendered actor-node map.

The same real-server actor snapshot contained no kind-`2` NPCs in the Four
Gates visibility set. This is recorded as a development-server/map population
limitation; it is not treated as evidence that rendered NPC activation or live
dialogue works.

### Verified bidirectional chat evidence: workflow run 33076142698

Commit `0f2798b127d269adac081c590bdd6d4d59862d29` passed the strict
headless job and the opt-in rendered development-server job. Artifact
`9647992715` records two distinct channel-`0` messages delivered through the
real server: the primary client submitted its marker through the normal chat UI
and the helper received it, then the helper sent its marker and the primary
reducer/UI received it.

Human inspection of `world-chat.png` confirmed that both sender-prefixed lines
and the chat entry are readable in the lower-left area, fully above the opaque
lower HUD rail. The first rendered attempt exposed the overlap and was not
accepted as visual evidence; the final layout reserves separate anchored bands
for history, entry, and the lower action rail. `chat.json` and the session log
contain `credentials: REDACTED`.

### Verified inventory, statistics, and equipment evidence: workflow run 33082783309

Commit `d9a8932415a8087f17c8171f44e3645d4d589f44` passed the strict
headless job and the opt-in rendered development-server job. Artifact
`9650864916` contains sanitized `inventory-stats.json` and `equipment.json`
plus human-inspected 1280x720 inventory, statistics, and equipped-fallback
captures.

The authoritative fresh-character snapshot contained three backpack entries
at slots 0 through 2 with image IDs `114`, `397`, and `460`, each at quantity
one. Real item-inspection responses identified them as Four Gates Guard Spear,
Guard Shield, and Guard Cape respectively, and each alias is pinned to matching
independent weapon, shield, or cloak artwork. All three inventory buttons and
all three corresponding item quick slots held visible icons and quantities.
The independent asset pack does not bundle the complete legacy Eternal Lands
item atlas range, so the registry resolves
those observed IDs through explicit data-driven Eloria substitutes and uses a
disclosed generic Eloria fallback for other unbundled IDs; tooltips retain the
authoritative legacy image ID instead of pretending the substitute is exact.

The server supplied health `20/20`, ether `32/32`, food `45`, carried/capacity
`20/80`, all six base attributes at `4`, and the complete skill snapshot. The
resource rail and statistics window presented those values. Human inspection
confirmed the inventory window fits above the lower HUD, the item and spell
quickbars remain readable, and the shifted statistics window no longer covers
the fixed resource rail. All six configured spell slots exposed availability
state. The fresh character had zero equipped items, so this run is not evidence
for a native equipment model. Artifact credentials remain `REDACTED`.

The same run selected the guard spear through the inventory UI and sent
`MOVE_INVENTORY_ITEM(20)` as `source_slot:u8 | destination_slot:u8`, moving
slot `0` to generic wear slot `36`. The server returned the authoritative
inventory/statistics snapshots and broadcast weapon part `0`, visual `112`.
The local luminous actor retained its native skeleton and created one visible
development fallback on the configured right-hand attachment. Human inspection
of `world-equipment-fallback.png` confirmed the bright fallback is visible on
the zoomed actor. Reversing the UI action moved slot `36` back to `0`, consumed
the server unwear update, removed the fallback node, and restored all three
backpack items. `equipment.json` records native count `0` and fallback count
`1 -> 0`; `data/actors/equipment.json` still contains no native GLB model
mappings, so this is explicit fallback evidence rather than native equipment
completion.
